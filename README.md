Installation:
```sh
git clone https://github.com/AntonHakansson/dotvim.git ~/.vim
```
Create symlinks
```sh
ln -s ~/.vim/vimrc ~/vimrc
```

Fetch submodules

```sh
git submodule init
git submodule update
```
